---
name: Build and publish Ansible Execution Environment

on:
  workflow_dispatch:
  schedule:
    - cron: '@daily'
  push:

jobs:
  include-common-workflow:
    uses: Element-Networks/ci_workflows/.forgejo/workflows/ansible_ee.yaml@main
    secrets: inherit
